# Usability Evaluation Summary

This folder records the qualitative evaluation evidence referenced in the thesis.

Current included file:

- `sus_responses.csv`: small pilot SUS-style response table.
- `sus_analysis.py`: computes SUS scores and writes `sus_summary.json`.

Run:

```bash
python3 evaluation/sus_analysis.py
```

Reporting note: before final thesis submission, replace the pilot rows with the actual participant responses collected under the approved class/capstone evaluation process. The current rows document the analysis format and make the thesis claim reproducible, but they should not be presented as a large-scale user study.
